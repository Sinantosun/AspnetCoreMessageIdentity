

  @*   <div class="col-lg-6">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox ">
                    <div class="ibox-title">
                        <h5>Son 5 mesajınızın durumları</h5>
                    </div>
                    <div class="ibox-content table-responsive">
                        <table class="table table-hover no-margins">
                            <thead>
                                <tr>
                                    <th>Gönderdiğiniz Kişi</th>
                                    <th>Mesaj Başlığı</th>
                                    <th>Durum</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><small>@item.Receiver.NameSurname</small></td>
                                        <td>@item.Subject</td>
                                        <td class="text-navy"> @(item.IsSenderMessageRead == true ? "Görüldü" : "İletildi")</td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div> *@

    <div class="col-lg-6">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox ">
                    <div class="ibox-title">
                        <h5>Henüz mesaj göndermediniz.</h5>
                    </div>
                </div>
            </div>

        </div>
    </div>

